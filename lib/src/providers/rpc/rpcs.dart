import 'package:flutter/widgets.dart';

//import 'package:yobi/src/providers/rpc/tank_branch.dart';
class RpcsProvider extends StatelessWidget {
  final Widget child;

  RpcsProvider({@required this.child});

  @override
  Widget build(BuildContext context) {
    return
        //MultiProvider(
        //  providers: [
        //...
//      ],
        //child:
        child;
    // );
  }
}
